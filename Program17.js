// WRITE NODE.JS CODE TO CREATE A MODULE CALLED mymodule WHICH CONTAIN (square() and cube()).

const myModule = require('./myModule');

console.log('Square of 2 is: ' + myModule.square(2));
console.log('Cube of 2 is: ' + myModule.cube(2));

